# Используем Node.js 18 в качестве базового образа
FROM node:18-alpine

# Устанавливаем рабочие каталоги
WORKDIR /app

# Копируем package.json и устанавливаем зависимости
COPY package*.json ./
RUN npm install

# Копируем все остальные файлы проекта
COPY . .

# Открываем порт 3000 для доступа к приложению
EXPOSE 3000

# Запускаем приложение
CMD ["npm", "start"]